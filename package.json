{
  "name": "hive-server",
  "version": "1.0.0",
  "description": "smart hive server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "mqtt": "^2.13.0"
  }
}
